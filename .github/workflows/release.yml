name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    name: Build Extensions
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chrome, edge, safari]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: '9'
          run_install: true
      - run: pnpm build:${{ matrix.browser }}
      - run: |
          cd build/${{ matrix.browser }}
          zip -r ../tab-workspace-${{ matrix.browser }}.zip .
      - name: Pack Chrome CRX
        if: matrix.browser == 'chrome'
        uses: cardinalby/webext-buildtools-chrome-crx-action@v2
        with:
          zipFilePath: build/tab-workspace-chrome.zip
          crxPath: build/tab-workspace-chrome.crx
          privateKey: ''
      - uses: actions/upload-artifact@v4
        with:
          name: tab-workspace-${{ matrix.browser }}
          path: build/tab-workspace-${{ matrix.browser }}.zip
      - name: Upload Chrome CRX
        if: matrix.browser == 'chrome'
        uses: actions/upload-artifact@v4
        with:
          name: tab-workspace-chrome-crx
          path: build/tab-workspace-chrome.crx

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: false
